---
const steps = [
  {
    number: '01',
    title: 'Analizamos tu negocio gratis',
    description: 'Vemos cómo trabajas y dónde estás perdiendo tiempo en tareas repetitivas.'
  },
  {
    number: '02',
    title: 'Plan de Automatización',
    description: 'Te decimos exactamente qué tareas se pueden automatizar y cuánto tiempo ganarás.'
  },
  {
    number: '03',
    title: 'Implementación',
    description: 'Ponemos en marcha el sistema mientras tú sigues con tu negocio normal.'
  },
  {
    number: '04',
    title: 'Ahorro Inmediato',
    description: 'Tú ahorras tiempo y dinero desde el primer día que el sistema empieza a funcionar.'
  }
];
---

<section id="metodologia" class="py-24 bg-dark relative">
  <div class="max-w-7xl mx-auto px-6">
    <div class="flex flex-col md:flex-row gap-16 items-start">
      
      <!-- Sticky Header -->
      <div class="md:w-1/3 md:sticky md:top-32">
        <h2 class="text-3xl md:text-5xl font-display font-bold mb-6">
          Cómo <br/><span class="text-gradient">Trabajamos</span>
        </h2>
        <p class="text-gray-400 text-lg mb-8">
          Nuestro método probado garantiza una transformación digital fluida y resultados medibles desde el primer mes.
        </p>
        <a href="#contacto" class="hidden md:inline-flex items-center gap-2 text-primary hover:text-white font-medium transition-colors group">
          Empezar ahora <span class="group-hover:translate-x-1 transition-transform">-></span>
        </a>
      </div>

      <!-- Steps Timeline -->
      <div class="md:w-2/3 space-y-12 relative before:absolute before:left-0 before:top-4 before:bottom-4 before:w-px before:bg-gradient-to-b before:from-primary before:to-transparent before:hidden md:before:block pl-0 md:pl-12">
        {steps.map((step, index) => (
          <div class="relative animate-on-scroll opacity-0 translate-x-8 transition-all duration-700" style={`transition-delay: ${index * 150}ms`}>
            <!-- Dot -->
            <div class="hidden md:block absolute -left-[54px] top-0 w-3 h-3 rounded-full bg-primary ring-4 ring-dark"></div>
            
            <div class="flex flex-col gap-4">
              <span class="text-6xl font-display font-bold text-white/5">{step.number}</span>
              <h3 class="text-2xl font-bold">{step.title}</h3>
              <p class="text-gray-400 leading-relaxed text-lg">{step.description}</p>
            </div>
          </div>
        ))}
        
        <div class="md:hidden mt-8">
            <a href="#contacto" class="inline-flex items-center gap-2 text-primary hover:text-white font-medium transition-colors group">
                Empezar ahora <span class="group-hover:translate-x-1 transition-transform">-></span>
            </a>
        </div>
      </div>

    </div>
  </div>
</section>

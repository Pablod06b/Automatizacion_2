---
import {
  Building2,
  Rocket,
  Briefcase,
  ShoppingBag,
  Users,
  Laptop,
} from "lucide-react";

const targets = [
  { label: "PYMES", icon: Building2 },
  { label: "Startups", icon: Rocket },
  { label: "Agencias", icon: Briefcase },
  { label: "E-commerce", icon: ShoppingBag },
  { label: "Consultores", icon: Users },
  { label: "Negocios Digitales", icon: Laptop },
];
---

<section id="target" class="py-24 bg-dark-light">
  <div class="max-w-7xl mx-auto px-6 text-center">
    <h2
      class="text-3xl md:text-5xl font-display font-bold mb-6 animate-on-scroll opacity-0 translate-y-8 transition-all duration-700"
    >
      Automatizamos procesos para <span class="text-gradient"
        >PYMES, E-commerce y Agencias</span
      >
    </h2>
    <p
      class="text-xl text-gray-400 max-w-3xl mx-auto mb-16 animate-on-scroll opacity-0 translate-y-8 transition-all duration-700"
      style="transition-delay: 100ms;"
    >
      Ahorra tiempo, reduce errores y consigue más clientes sin levantar un
      dedo.
    </p>

    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3 md:gap-8">
      {
        targets.map((target, index) => (
          <div
            class="flex flex-col items-center gap-3 p-4 md:p-6 rounded-2xl bg-white/5 border border-white/5 hover:border-primary/30 hover:bg-white/10 transition-all duration-300 hover:-translate-y-1 animate-on-scroll opacity-0 scale-90"
            style={`transition-delay: ${index * 100}ms`}
          >
            <div class="w-10 h-10 md:w-12 md:h-12 rounded-full bg-dark flex items-center justify-center text-primary mb-1 md:mb-2">
              <target.icon className="w-5 h-5 md:w-6 md:h-6" />
            </div>
            <span class="text-sm md:text-base font-medium text-gray-300">
              {target.label}
            </span>
          </div>
        ))
      }
    </div>
  </div>
</section>
